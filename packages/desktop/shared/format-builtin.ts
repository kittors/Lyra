/**
 * 「这一份是谁格式化的」——内置引擎的那个答案。
 *
 * 是个标记，不是一句话。主进程的 `format-external.ts` 决定用哪个引擎，界面在 `CodeEditor` 上把结果
 * 念出来，而中间隔着一次 IPC——**能翻译的只有界面那一端**。
 *
 * 这里原本直接传一个中文的「内置」过去，于是英文、日文、法文界面保存一个 `.go` 文件，提示是
 * `Formatted with 内置`。同一次改动其实已经给七种语言都补上了 `format.builtin`，只是那个键接在了
 * 「外部工具没装、退回内置」的兜底分支上，成功路径够不着它。
 *
 * 放在 `shared/` 是因为两个进程都要认得它：一个写，一个读。写死两份字符串是同一个 bug 的下一种形态。
 */
export const BUILTIN_FORMATTER = "@builtin";
