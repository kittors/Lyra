{
  "name": "@lyra/agent-cli",
  "version": "0.8.36",
  "private": true,
  "type": "module",
  "description": "The agent that works on this repository, run from CI.",
  "bin": {
    "lyra-agent": "./src/main.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "@lyra/core": "workspace:*"
  }
}
