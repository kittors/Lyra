{
  "name": "@lyra/contract",
  "version": "0.8.36",
  "private": true,
  "type": "module",
  "description": "The boundary between the renderer and the main process: what can be called, and from where",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b tsconfig.json",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "@types/node": "24.13.3"
  }
}
